#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
 * main - Print single digit of base 10 numbers from 0
 *
 * Return: always 0 (success)
 */
int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
	{
		printf("%d\n", i);
	}
	printf("\n");
	return (0);
}

